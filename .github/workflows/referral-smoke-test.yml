name: Referral smoke test

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  branch-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Referral smoke test is disabled on the temporary Bing diagnostic branch."
