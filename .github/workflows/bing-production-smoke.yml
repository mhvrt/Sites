name: Bing production smoke

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  bing-home-controls:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --ignore-scripts --no-audit --no-fund
      - run: npx playwright install --with-deps chromium
      - name: Inspect live Bing controls
        env:
          REPORT_PATH: ${{ runner.temp }}/bing-home-controls.json
        run: node scripts/debug-bing-home-controls.mjs
      - name: Show report
        if: always()
        run: cat "${{ runner.temp }}/bing-home-controls.json" || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: bing-home-controls-${{ github.run_id }}
          path: ${{ runner.temp }}/bing-home-controls.json
          if-no-files-found: warn
