name: Bing production smoke

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: read

jobs:
  bing-pagination:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: npm install --ignore-scripts --no-audit --no-fund
      - run: npx playwright install --with-deps chromium firefox
      - name: Inspect Bing pagination
        env:
          REPORT_PATH: ${{ runner.temp }}/bing-serp-pagination.json
        run: node scripts/debug-bing-serp-pagination.mjs
      - name: Show report
        if: always()
        run: cat "${{ runner.temp }}/bing-serp-pagination.json" || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: bing-pagination-${{ github.run_id }}
          path: ${{ runner.temp }}/bing-serp-pagination.json
          if-no-files-found: warn
